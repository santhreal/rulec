# BACKLOG - rulec

Seeded 2026-07-14 (main-thread audit; Gemini swarm was quota/error-blocked). One-way YARA→vyre rule compiler. Exemplary discipline: every unsupported construct is rejected loudly with what/why/fix (never a silent subset), condition consumers are exhaustive matches (ONE PLACE), and the differential oracle counts every skip. Findings are capability/consistency, not correctness breaks. Rows: `file:line | category | severity | problem | acceptance criteria`.

DONE 2026-07-14 | src/lower/mod.rs:172 | capability | medium | `lower_ast` does `ast.rules().map(lower_rule).collect::<Result<Vec,_>>()` - all-or-nothing: the FIRST rule with an unsupported construct (`xor`, `fullword`, private, rule-refs) aborts the entire ruleset and discards every already-lowered rule. Real YARA corpora (hundreds of rules) almost always contain at least one such rule, so a whole pack fails to compile. The rejection is loud (good) but there is no way to compile the supported subset. | add a partial-compile entry point returning `(Vec<LoweredRule>, Vec<(rule_name, LowerError)>)` so supported rules lower while each rejection is reported per-rule; keep the strict all-or-nothing `lower_ast` for callers that want it. Test on a mixed corpus (some supported, some `xor`) asserting the supported ones compile and the rejects are listed. | FIXED: added `lower::lower_ast_partial` returning `(Vec<LoweredRule>, Vec<(String, LowerError)>)` and `rulec::compile_partial` returning `PartialCompiled { rules, srg, lower_errors }`; `compile` stays strict all-or-nothing. Tests `lower_ast_partial_lowers_supported_rules_and_reports_rejects` and `lower_ast_partial_empty_source_produces_empty_results`. `cargo +1.92.0 test -p rulec --lib` CARGO_EXIT=0 (4 passed).
DONE 2026-07-14 | src/oracle.rs:199 | test-truth | medium | `build_oracle` does `compiler.add_source(src).ok()?` - when the REFERENCE yara-x compiler rejects the source, it returns `None` and the file is counted only as coarse `no_oracle`, discarding yara-x's compile error. But if rulec SUCCESSFULLY lowered a rule that yara-x refuses to compile, that means rulec is MORE permissive than YARA (a soundness concern) and it is silently folded into a generic bucket rather than flagged. | capture the yara-x compile error; when rulec lowered a rule that yara-x rejected, record it as a distinct "rulec-accepts-but-yara-rejects" soundness class in the report, not `no_oracle`. Assert the class is populated on a crafted rule. | FIXED: changed `build_oracle` to return `Result<Option<yara_x::Rules>, String>` and propagate yara-x compile errors instead of discarding them with `.ok()?`. `CorpusReport` gained `rulec_accepts_yara_rejects` and `yara_reject_examples`; `scan_file` now counts every lowered rule in a file yara-x rejected as the soundness gap. `render` surfaces the count. Added `oracle::tests::{build_oracle_reports_yara_compile_error, scan_file_counts_rulec_accepts_yara_rejects, scan_file_uses_oracle_when_yara_compiles}` using a crafted YARA source with an invalid regex (accepted by rulec's parser/lowering, rejected by yara-x's compiler). `cargo +1.92.0 test -p rulec --lib --features differential` and `--test differential --features differential` CARGO_EXIT=0.
DONE 2026-07-14 | src/eval.rs:131 | correctness | medium | The CPU reference `eval` saturates match counts with `u32::try_from(re.find_iter(data).count()).unwrap_or(u32::MAX)`, but the vyre engine caps at `DEFAULT_MAX_MATCHES = 10_000` (vyre_engine.rs:101). For a condition like `#a >= 10_001` on a buffer with >10k matches, the CPU oracle (real/saturated count) and vyre (capped at 10k) disagree, producing a FALSE divergence in the differential harness (or masking a real one). The two reference count-models must share one cap. | make the CPU `eval` apply the same `DEFAULT_MAX_MATCHES` cap as vyre (single owner for the cap), so both references agree; add a test with a high-count buffer and a threshold straddling the cap. | FIXED: `count_regex` now caps regex match counts at `DEFAULT_MAX_MATCHES = 10_000` (shared constant in `eval.rs`, re-exported from `vyre_engine.rs`), matching the vyre engine cap. Added `regex_match_count_capped_at_default_max` and `regex_match_count_under_cap_is_exact` tests.
DONE 2026-07-14 | src/lower/patterns.rs:205 | robustness | low | `PatKind::Text(forms.into_iter().next().expect("one form"))` uses `.expect()` (a panic path) in a crate whose whole thesis is "reject loudly, never panic/silent." It is guarded-unreachable (`encodings` always yields ≥1 form and this arm is `forms.len()==1`), but the panic is inconsistent with the crate's contract. | replace with `unreachable!("encodings guarantees >=1 form")` and a one-line justification, or restructure so the invariant is expressed in the type; confirm no `expect/unwrap` remain on the lowering path. | FIXED: replaced the `.expect("one form")` with `unwrap_or_else(|| unreachable!("encodings guarantees >=1 form"))` plus an inline comment explaining `encodings` always yields at least one form because `read_mods` defaults to ascii. Confirmed `lower/patterns.rs` has no other `.expect`/`.unwrap` calls on the lowering path. `cargo +1.92.0 test -p rulec --lib` green (CARGO_EXIT=0).
DONE 2026-07-14 src/oracle.rs:206 | test-gap | low | The differential harness probes each rule with only two synthetic buffers: `empty` and `all-literals` concatenation. Regex-only patterns are never synthesised into the buffer (comment at 264 acknowledges this), so a rule whose match hinges on a regex pattern is only ever checked against buffers where that regex is absent - the "present" verdict path for regex patterns is under-exercised. | add a probe that synthesises a matching input for at least simple regex patterns (or a curated positive-sample buffer), so the regex-present branch is differentially validated, not just the regex-absent branch. | DONE 2026-07-14: added a third differential probe `all-patterns` and `synthesize_regex_match` in `oracle.rs`. The synthesizer builds a heuristic candidate by dropping `^`/`$` anchors, keeping alphanumerics, and replacing metacharacters with `0`, then trims from the right until the regex matches; it falls back to a bounded brute-force search over `a-z0-9` up to length 5. `all_patterns_buffer` concatenates literal/hex bytes plus a synthesised match for each regex. Added `synthesize_regex_match_finds_for_simple_regexes` and `all_patterns_buffer_fires_regex_present_path` tests. `cargo +1.92.0 test --features differential` passed (31 passed).
src/oracle.rs:420 | rulec | silent-fallback | medium | REPOPULATE 2026-07-16: std::fs::read_dir(dir) error on directory read failure is quietly discarded, returning an empty or partial file list, downstream causing run_corpus to scan fewer files or skip entire directories silently without any warning | surface loudly: surface the read error by returning a Result or recording it under read_errors, plus a proving test asserting that calling run_corpus with an unreadable path increments read_errors or returns the error | FIXED (Law-10): `collect_yara_files` now takes a `read_errors: &mut usize` and increments it on the `read_dir` Err arm instead of a silent `return`; the recursion threads the same counter. Added a dedicated `CorpusReport::dir_read_errors` field (distinct from file `read_errors` so directory-walk vs file-read failures are not conflated) seeded from the walk in `run_corpus`, and surfaced it in the report summary ("read-error dirs"). Proving test oracle::tests::run_corpus_counts_unreadable_directory_instead_of_silent_skip asserts run_corpus on a nonexistent directory yields files==0 and dir_read_errors==1. Gate: cargo test --features differential, 31 lib tests passed, EXIT=0. | status=done
src/oracle.rs:290 | rulec | performance | high | REPOPULATE 2026-07-16: synthesize_regex_match executes a bounded brute-force search over a 36-character alphabet for string lengths up to 5 when the heuristic fails, performing 60 million iterations of re.is_match, downstream causing the differential testing harness to block for up to several minutes on regexes that cannot match the lowercase alphabet | reduce complexity: decrease the maximum brute-force search length or exit early, plus a proving test asserting that calling synthesize_regex_match with a non-matching uppercase regex (like ^[A-Z]{5}$) runs in less than 10 milliseconds | FIXED (Law-7): added a `MAX_BRUTE_FORCE_ATTEMPTS = 50_000` total-candidate budget across all lengths in `synthesize_regex_match`; the brute-force loop counts attempts and returns None once exceeded, so an unsatisfiable regex bails in <10ms instead of walking the full 36^5 ≈ 60M space (minutes). Budget chosen so lengths 1-3 (47_988 candidates) stay exhaustive AND length-4 homogeneous matches (`aaaa`, the first length-4 candidate) are still reached - no capability regression for realistic short matches. Best-effort: a skipped probe is surfaced by the caller's counters, never a silent hang. Proving tests oracle::tests::synthesize_regex_match_bails_fast_on_unsatisfiable_alphabet (asserts `^[A-Z]{5}$` returns None in <10ms) and synthesize_regex_match_still_covers_length_four_classes (asserts `[a-z]{4}` still synthesizes a matching sample). Gate: cargo test --features differential, 31 lib tests passed, EXIT=0. | status=done
